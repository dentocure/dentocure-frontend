// export const getTenantFromURL = (hostname) => {
//   const parts = hostname.split('.');
//   if (parts.length > 2) return parts[0];
//   return 'default';
// };
 export const getTenantFromURL = () => 'clinic1'; 